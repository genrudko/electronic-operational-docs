# Copy to /srv/eod/secrets/preview.env on the VPS.
# Generate secret values locally. Never commit the populated file.
DJANGO_SECRET_KEY=
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost

POSTGRES_DB=eod_preview
POSTGRES_USER=eod_preview
POSTGRES_PASSWORD=

EOD_PREVIEW_PORT=8765
TIME_ZONE=Europe/Moscow
